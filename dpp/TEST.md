# Test de la route DPP

Si vous voyez ce message, la route `/api/dpp` fonctionne mais ne peut pas lire le fichier index.html.

Vérifiez que le fichier existe dans `public/vocabularies/dpp/index.html`

